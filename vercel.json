{
  "version": 2,
  "buildCommand": "npm run build --workspace packages/shared && npm run build --workspace apps/api",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}